export default theme => ({
  list: {
    width: '100%',
    maxWidth: 420,
    backgroundColor: theme.palette.background.paper,
    position: 'relative',
    overflowX: 'hidden',
    overflowY: 'scroll',
    maxHeight: 600,
  },
});